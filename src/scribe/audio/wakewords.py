"""openWakeWord spotters for commit / undo / scratch-that, running
continuously on the raw stream in parallel with VAD. Spotter hits bypass the
engine entirely -> app intent frame. Training scripts live in tools/wakewords/
(piper-sample-generator; negatives = recorded math dictation).
Implement in PLAN.md Phase 5."""
# TODO(Phase 5)
